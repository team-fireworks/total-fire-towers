base: roblox
name: selene-std
globals:
  # Allow assertions without reasoning
  assert:
    args:
    - type: any
    - required: false
      type: string
  # Override Roblox require style with string requires
  require:
    args:
      - type: any
